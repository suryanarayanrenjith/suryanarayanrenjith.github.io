{
  "name": "suryanarayanrenjith-github-io",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Personal portfolio — built with Vite for production minification and GitHub Pages deployment.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "clean": "node -e \"import('node:fs').then(fs => fs.promises.rm('dist', { recursive: true, force: true }))\""
  },
  "devDependencies": {
    "html-minifier-terser": "^7.2.0",
    "terser": "^5.36.0",
    "vite": "^5.4.10",
    "vite-plugin-static-copy": "^2.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
