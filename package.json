{
  "name": "suryanarayanrenjith-github-io",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Surya Website — built with Vite for production minification and GitHub Pages deployment.",
  "scripts": {
    "build": "vite build",
    "clean": "node scripts/clean.mjs",
    "dev": "node scripts/refuse.mjs dev",
    "preview": "node scripts/refuse.mjs preview",
    "serve": "node scripts/refuse.mjs serve",
    "start": "node scripts/refuse.mjs start",
    "test": "node scripts/refuse.mjs test"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "html-minifier-terser": "^7.2.0",
    "terser": "^5.36.0",
    "vite": "^6.4.2",
    "vite-plugin-static-copy": "^2.2.0"
  },
  "overrides": {
    "esbuild": "^0.25.0"
  },
  "engines": {
    "node": ">=20"
  }
}
